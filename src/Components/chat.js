// create a chat app in ReactJs which sends chat messages
// to the server and receives them back. The user can type into an input field,
// press enter to send the message and receive new messages from the server.
import React, { Component } from "react";

class ChatApp extends Component {
  render() {
    return (
      <div>
        <h2></h2>
      </div>
    );
  }
}
export default ChatApp;
